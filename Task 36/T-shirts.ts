function make_shirt(size: string, color: string, message: string) {
    console.log(`Making a ${size} size shirt in ${color} color with the message: "${message}"`);
  }
  
  // Example usage
  make_shirt('medium', 'black','Sheikh Shaheer');
  